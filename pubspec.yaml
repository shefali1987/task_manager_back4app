name: task_manager_back4app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0-133.0.dev

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Parse Server SDK for Flutter (compatible with idb_shim)
  parse_server_sdk_flutter: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

dependency_overrides:
  idb_shim: ^2.6.7+1
  sembast: ^3.0.0
  sembast_web: ^2.4.2

